# Python
__pycache__/
*.py[cod]
*$py.class

# IDE / VS Code
.vscode/
.idea/
.vs/

# Bot folders containing sensitive user credentials and temporary logs
input-id/
login-success/
backup/
fast-random/
backup-id/
logs/
debug-ocr/
cap-id-result/
check-coin/
coin*+/

# Dat files / zip archives / installer files
*.dat
*.zip
*.exe

# Tesseract — ไม่เอาขึ้น git แล้ว (28 MB จาก 38 MB ของทั้งรีโปอยู่ตรงนี้)
# ทำให้เครื่องลูกโหลดอัปเดตช้าจน timeout — ติดตั้งครั้งเดียวต่อเครื่องที่
# C:\Program Files\Tesseract-OCR (โค้ดอ้างพาธนี้อยู่แล้ว ไม่ได้ใช้โฟลเดอร์ในรีโป)
Tesseract-OCR/
